# Worklog — 2026-07-11

### Code Review 后修复 (6 项)

- **类型：** Bug修复 / 重构
- **范围：**
  - `backend/.../application/UserApplicationService.java`
  - `backend/.../domain/User.java`
  - `backend/.../domain/UserDomainService.java`
  - `backend/.../infrastructure/UserRepository.java`
  - `backend/.../presentation/UserController.java`
  - `backend/.../presentation/dto/UpdateUserRequest.java`
  - `backend/.../config/GlobalExceptionHandler.java`
  - `database/migration/V003__partial_unique_indexes.sql`
  - `frontend/src/pages/UserEditPage.tsx`
- **摘要：** 修复 review 发现的 7 个问题

**修复清单：**

1. **deleteUser 添加事务包裹** — 与 createUser/updateUser 一致，`.as(transactionalOperator::transactional)`
2. **recordLoginFailure 使用 changeStatus + isLocked 使用 changeStatus** — 不再直接赋值 `this.status =`，通过状态机校验
3. **UserEditPage 添加 username 编辑字段** — 前后端一致
4. **管理员 LOCKED 设置 lockedUntil** — 30 分钟超时，与自动锁定一致
5. **分页参数校验** — `@Min(1)` on page，`@Min(1) @Max(100)` on size，添加 `ConstraintViolationException` handler
6. **错误码细化** — 40001 参数校验 / 40010 密码强度 / 40011 状态转换非法
7. **方案A：已删除用户标识符可复用** — `findByUsername`/`findByEmail` 过滤 DELETED，V003 partial unique index

### 验证结果

- `./mvnw test` — 25 tests 全绿
- `./mvnw compile` — BUILD SUCCESS
- `npm run build` — built in 4.88s
- E2E: 已删除用户名复用 → 200, 重复活跃用户名 → 40002, DISABLED→LOCKED → 40011, page=0&size=200 → 40001

---
**00:12** — 会话结束

### UI 全面优化

- **类型：** 重构 / 新功能
- **范围：**
  - `frontend/src/styles/global.css` — 新增全局设计系统
  - `frontend/src/main.tsx` — 添加 Ant Design ConfigProvider 主题定制
  - `frontend/src/layouts/MainLayout.tsx` — 重写布局
  - `frontend/src/pages/HomePage.tsx` — Dashboard 页面重写
  - `frontend/src/pages/UserListPage.tsx` — 用户列表页优化
  - `frontend/src/pages/UserCreatePage.tsx` — 创建页优化
  - `frontend/src/pages/UserEditPage.tsx` — 编辑页优化
  - `frontend/src/i18n/locales/zh-CN.json` — 新增 menu/breadcrumb/header/footer 键
  - `frontend/src/i18n/locales/ja-JP.json` — 同上
  - `frontend/src/i18n/locales/en-US.json` — 同上
- **摘要：** 全面 UI/UX 优化

**优化详情：**

1. **全局设计系统（global.css）** — 自定义滚动条、页面入场动画（fadeIn + translateY）、卡片 hover 抬升效果（hover-lift）、统计卡片渐变色图标容器（stat-icon--blue/green/purple/orange）、侧边栏 Logo 渐变文字、响应式断点
2. **Ant Design 主题定制** — `borderRadius: 8`、`borderRadiusLG: 12`、中日英系统字体栈、暗色菜单选中项使用主色、表格表头背景色
3. **MainLayout 重写** — 固定侧边栏 + 动态 marginLeft、Logo 区（盾牌图标 + 渐变文字）、自动面包屑导航（根据路由路径动态生成）、用户头像下拉菜单（Header 右侧）、语言切换器移至侧边栏底部（展开：Radio.Button 中/日/EN；收起：仅图标）
4. **Dashboard** — 四张统计卡片使用渐变色图标容器（蓝/绿/紫/橙）、hover 抬升效果、加载态显示 Skeleton 而非 Spin、欢迎卡片双栏布局（左侧标题+描述，右侧快捷操作按钮）
5. **用户列表页** — 页头区域（标题 + 创建按钮 + 面包屑由 MainLayout 提供）、搜索栏 + 表格整体用 Card 包裹、错误提示可关闭、分页增加 showQuickJumper
6. **创建/编辑表单页** — `maxWidth: 760px` 居中卡片、表单分节（账户信息 / 个人信息 / 状态）+ Divider 分隔、`size="large"` 输入框、requiredMark="optional"、底部按钮右对齐

**设计决策：**
- 语言切换保留 `window.location.reload()` 以同步 Ant Design 组件内置文案（DatePicker、Pagination 等）
- 未实现模块（角色/租户/团队）菜单项设为 `disabled: true`
- 面包屑由 MainLayout 根据 `location.pathname` 自动计算，子页面不重复渲染


---
**00:22** — 会话结束

---

### UI Review + 自建重构

- **类型：** 重构
- **范围：**
  - `CLAUDE.md` — 新增 Product UI/UX Rules 章节
  - `.claude/skills/ui-review/SKILL.md` — 新建 UI Review Skill
  - `.mcp.json` — 新建 Playwright MCP 配置
  - `frontend/src/i18n/locales/zh-CN.json` — 新增 dashboard/form placeholder 键
  - `frontend/src/i18n/locales/ja-JP.json` — 同上
  - `frontend/src/i18n/locales/en-US.json` — 同上
  - `frontend/src/layouts/MainLayout.tsx` — 面包屑改用 Ant Design Breadcrumb 组件、用户区简化
  - `frontend/src/pages/HomePage.tsx` — 加副标题、未实现卡片加"即将上线"标签、去 hover-lift
  - `frontend/src/pages/UserListPage.tsx` — 空状态用 Empty 组件、页头统一、操作列 fixed
  - `frontend/src/pages/UserCreatePage.tsx` — 去双标题、节标题 i18n、placeholder i18n
  - `frontend/src/pages/UserEditPage.tsx` — 去双标题、Loading 用 Skeleton、节标题 i18n
- **摘要：** 按 SaaS 产品标准自审并修复 12 个问题（3 P0 + 7 P1 + 2 P2）

**P0 修复：**
1. 面包屑从裸 HTML 改为 `<Breadcrumb items={...} />` — 标准样式 + 可访问性
2. 用户区移除 disabled 下拉菜单项，改为纯头像展示（auth 未实现前不暴露无效交互）
3. CreatePage / EditPage 移除 Card 内的 `<Title>` 双标题

**P1 修复：**
4. Dashboard 添加 `subtitle` 副标题
5. 未实现统计卡片加 `comingSoon` 标签
6. UserListPage 页头统一用 `.page-header` 类
7. 空状态用 `<Empty>` 组件 + 描述 + 引导文案
8. 表单 placeholder 全部 i18n（usernamePlaceholder 等）
9. 表单节标题用专用 `form.section.*` 键
10. EditPage Loading 状态改为 Skeleton

**P2 修复：**
11. 统计卡片去掉 hover-lift（不可点击不应有手型光标）
12. EditPage Divider 间距统一为 24px

**工具链升级：**
- Playwright MCP 已安装并配置（`@playwright/mcp` + `.mcp.json`）
- UI Review Skill 已创建（`/ui-review`）
- CLAUDE.md 新增 Product UI/UX Rules（设计原则 + 页面规则 + 组件规范 + 自检清单）
- Build: ✓ built in 5.60s

---
**00:28** — 会话结束

---

### 文档完善 + GitHub 上线 + 分支规范

- **类型：** 文档 / DevOps
- **范围：**
  - `README.md` — 重写为 13 章节详细文档
  - `docs/progress.md` — 新建项目进度报告
  - `docs/branching-strategy.md` — 新建分支管理规范（9 章节）
  - `frontend/src/layouts/MainLayout.tsx` — 修复 toast 硬编码、恢复用户下拉
  - `frontend/src/i18n/locales/*.json` — 新增 header.logoutHint 键
- **摘要：** 文档体系完善 + Git 仓库上线 + 分支策略三版修正

**文档体系：**

| 文档 | 内容 |
|------|------|
| README.md | 系统架构图 + API 文档 + 数据库设计 + 前端架构 + i18n 指南 + 开发工作流 + 编码规范 + FAQ |
| docs/progress.md | 后端/数据库/前端/工具链四大板块进度 + 下一步 P0/P1/P2 |
| docs/branching-strategy.md | 4 长期分支 + 7 种临时分支 + 流向图 + 合并矩阵 + 4 场景工作流 |

**分支策略最终版（三版修正）：**
- 核心铁律：`dev` 代码只进不出，绝不流向任何长期分支
- 所有 task 分支从 `master`（唯一稳定基线）拉
- task → dev（测试）→ task → release → prod → master
- release 从 `master` 创建，只合入测试通过的 task 分支

**GitHub 仓库：** https://github.com/Low-study/ai-fms
- 4 个分支：master / dev / release / prod
- 4 次 commit 已推送

**修复项：**
- 语言切换按钮：`中/日/EN` → `ZH/JA/EN`（ISO 639-1）
- 用户下拉恢复：logout 可点击，toast 提示开发中（已 i18n）
- 表单 placeholder 全部 i18n 化
- Toast 硬编码中文 → `t('header.logoutHint')`
